#include <iostream>
#include <cmath>
using namespace std;

// 1
/*
int main()
{
    float x, y;
    float pi = 3.14;
    cin >> x;
    cout << "\n";
    y = sqrt(exp(2 * x)) - fabs(sin(pi * x / x + 2 / 3)) + 1.7;
    cout << "y= " << y;
    return 0;
}
*/

// 2

/*
int main()
{
    float x, y;
    cin >> x;
    cout << "\n";
    y = log(fabs(x - 1.5)) + sqrt(pow(x, 4)) + sqrt(exp(3 * x) + 4);
    cout << "y= " << y;
    return 0;
}
*/

// 3

int main()
{
    float x, y;
    cin >> x;
    cout << "\n";
    y = tan(5 / 3) * x + log(fabs(2 * x + 5)) + 1.8;
    cout << "y= " << y;
    return 0;
}
